# Project test việc sử dụng raylib để vẽ GUI

## Build raylib

clone raylib từ source sau

```bash
git clone https://github.com/raysan5/raylib.git
cd raylib-src/src
make PLATFORM=PLATFORM_DESKTOP
```

copy 2 file raylib.h và raymath.h vào thư mục include, copy file libraylib.a vào thư mục lib

---

# Cấu Hình C/raylib trên macOS + VSCode

## File JSON

### 1. `.vscode/tasks.json` - Cấu Hình Biên Dịch

```json
{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C/C++: clang build file đang mở",
      "command": "/usr/bin/clang",
      "args": [
        "-I${workspaceFolder}/include",
        "-L${workspaceFolder}/lib",
        "-fcolor-diagnostics",
        "-fansi-escape-codes",
        "-g",
        "${file}",
        "-o",
        "${workspaceFolder}/build/${fileBasenameNoExtension}",
        "-lraylib",
        "-framework",
        "CoreVideo",
        "-framework",
        "IOKit",
        "-framework",
        "Cocoa",
        "-framework",
        "CoreFoundation",
        "-framework",
        "AppKit"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": "build",
      "detail": "Task generated by Debugger."
    }
  ],
  "version": "2.0.0"
}
```

### 2. `.vscode/launch.json` - Cấu Hình Debug

```json
{
  "configurations": [
    {
      "name": "C/C++: clang build và debug file đang mở",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/${fileBasenameNoExtension}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "C/C++: clang build file đang mở"
    }
  ],
  "version": "2.0.0"
}
```

## 📚 Giải Thích Ngắn Gọn

### tasks.json - Các Tham Số Quan Trọng

| Tham Số                                | Ý Nghĩa                        |
| -------------------------------------- | ------------------------------ |
| `-I${workspaceFolder}/include`         | Compiler tìm `raylib.h` ở đây  |
| `-L${workspaceFolder}/lib`             | Linker tìm `libraylib.a` ở đây |
| `-lraylib`                             | Link với raylib library        |
| `-framework CoreVideo/IOKit/Cocoa/...` | Link với macOS frameworks      |
| `-g`                                   | Thêm debug symbols (để debug)  |
| `"preLaunchTask"` (trong launch.json)  | Auto rebuild trước khi debug   |

### Tại Sao Cần 5 Frameworks?

raylib gọi macOS APIs để tạo cửa sổ, đọc input, quản lý video. Các frameworks này chứa các APIs đó:

- **Cocoa** - Tạo cửa sổ
- **AppKit** - Quản lý app lifecycle & events
- **CoreFoundation** - Utilities (strings, arrays, dictionaries)
- **CoreVideo** - Video output & frame rate
- **IOKit** - Đọc joystick/gamepad

Nếu thiếu chúng, linker báo lỗi: `Undefined symbols`

### Quy Trình Khi Nhấn F5

```
1. VSCode chạy task "clang build active file" từ tasks.json
2. Clang compile main.c → main.o
3. Clang link main.o + libraylib.a + 5 frameworks → executable "main"
4. VSCode launch LLDB debugger chạy "main"
5. Chương trình hiển thị trên màn hình
```

## Để đóng gói thành file cài đặt cho MacOS

chạy file sh sau

```
chmod +x scripts/package_mac_app.sh
./scripts/package_mac_app.sh
```
